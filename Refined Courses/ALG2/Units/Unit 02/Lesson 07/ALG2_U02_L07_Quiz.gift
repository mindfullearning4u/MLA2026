::ALG2_U02_L07_Q01::
Question ID:<br>ALG2_U02_L07_Q01<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Solve x<sup>2</sup> - 6x + 8 = 0. {
= x = 2 and x = 4#Correct. Factoring gives (x - 2)(x - 4) = 0.
~ x = -2 and x = -4#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x = 6 and x = 8#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x = 0 and x = 8#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q02::
Question ID:<br>ALG2_U02_L07_Q02<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Solve x<sup>2</sup> + x - 12 = 0. {
~ x = 4 and x = -3#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= x = -4 and x = 3#Correct. Factoring gives (x + 4)(x - 3) = 0.
~ x = 1 and x = -12#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x = 0 and x = -12#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q03::
Question ID:<br>ALG2_U02_L07_Q03<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Which solves x<sup>2</sup> - 16 > 0? {
~ -4 < x < 4#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ -4 <= x <= 4#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= x < -4 or x > 4#Correct. The upward-opening graph is above the x-axis outside the zeros.
~ all real numbers#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q04::
Question ID:<br>ALG2_U02_L07_Q04<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Which solves x<sup>2</sup> - 25 <= 0? {
~ x < -5 or x > 5#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x <= -5 or x >= 5#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ all real numbers#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= -5 <= x <= 5#Correct. The graph is on or below the x-axis between and including the zeros.
}

::ALG2_U02_L07_Q05::
Question ID:<br>ALG2_U02_L07_Q05<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>For y < x<sup>2</sup> - 3, which statement is true? {
= Use a dashed boundary and shade below.#Correct. Strict inequality uses a dashed boundary, and y less than means below.
~ Use a solid boundary and shade below.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ Use a dashed boundary and shade above.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ Use a solid boundary and shade above.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q06::
Question ID:<br>ALG2_U02_L07_Q06<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>For y >= -x<sup>2</sup> + 1, which statement is true? {
~ Use a dashed boundary and shade above.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= Use a solid boundary and shade above.#Correct. The symbol >= includes the boundary and represents y-values above it.
~ Use a solid boundary and shade below.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ Use no boundary.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q07::
Question ID:<br>ALG2_U02_L07_Q07<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Solve x<sup>2</sup> - 6x + 8 = 0. {
~ x = -2 and x = -4#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x = 6 and x = 8#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= x = 2 and x = 4#Correct. Factoring gives (x - 2)(x - 4) = 0.
~ x = 0 and x = 8#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q08::
Question ID:<br>ALG2_U02_L07_Q08<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Solve x<sup>2</sup> + x - 12 = 0. {
~ x = 4 and x = -3#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x = 1 and x = -12#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x = 0 and x = -12#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= x = -4 and x = 3#Correct. Factoring gives (x + 4)(x - 3) = 0.
}

::ALG2_U02_L07_Q09::
Question ID:<br>ALG2_U02_L07_Q09<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Which solves x<sup>2</sup> - 16 > 0? {
= x < -4 or x > 4#Correct. The upward-opening graph is above the x-axis outside the zeros.
~ -4 < x < 4#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ -4 <= x <= 4#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ all real numbers#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q10::
Question ID:<br>ALG2_U02_L07_Q10<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Which solves x<sup>2</sup> - 25 <= 0? {
~ x < -5 or x > 5#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= -5 <= x <= 5#Correct. The graph is on or below the x-axis between and including the zeros.
~ x <= -5 or x >= 5#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ all real numbers#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q11::
Question ID:<br>ALG2_U02_L07_Q11<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>For y < x<sup>2</sup> - 3, which statement is true? {
~ Use a solid boundary and shade below.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ Use a dashed boundary and shade above.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= Use a dashed boundary and shade below.#Correct. Strict inequality uses a dashed boundary, and y less than means below.
~ Use a solid boundary and shade above.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q12::
Question ID:<br>ALG2_U02_L07_Q12<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>For y >= -x<sup>2</sup> + 1, which statement is true? {
~ Use a dashed boundary and shade above.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ Use a solid boundary and shade below.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ Use no boundary.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= Use a solid boundary and shade above.#Correct. The symbol >= includes the boundary and represents y-values above it.
}

::ALG2_U02_L07_Q13::
Question ID:<br>ALG2_U02_L07_Q13<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Solve x<sup>2</sup> - 6x + 8 = 0. {
= x = 2 and x = 4#Correct. Factoring gives (x - 2)(x - 4) = 0.
~ x = -2 and x = -4#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x = 6 and x = 8#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x = 0 and x = 8#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q14::
Question ID:<br>ALG2_U02_L07_Q14<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Solve x<sup>2</sup> + x - 12 = 0. {
~ x = 4 and x = -3#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= x = -4 and x = 3#Correct. Factoring gives (x + 4)(x - 3) = 0.
~ x = 1 and x = -12#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x = 0 and x = -12#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q15::
Question ID:<br>ALG2_U02_L07_Q15<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Which solves x<sup>2</sup> - 16 > 0? {
~ -4 < x < 4#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ -4 <= x <= 4#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= x < -4 or x > 4#Correct. The upward-opening graph is above the x-axis outside the zeros.
~ all real numbers#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q16::
Question ID:<br>ALG2_U02_L07_Q16<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Which solves x<sup>2</sup> - 25 <= 0? {
~ x < -5 or x > 5#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x <= -5 or x >= 5#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ all real numbers#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= -5 <= x <= 5#Correct. The graph is on or below the x-axis between and including the zeros.
}

::ALG2_U02_L07_Q17::
Question ID:<br>ALG2_U02_L07_Q17<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>For y < x<sup>2</sup> - 3, which statement is true? {
= Use a dashed boundary and shade below.#Correct. Strict inequality uses a dashed boundary, and y less than means below.
~ Use a solid boundary and shade below.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ Use a dashed boundary and shade above.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ Use a solid boundary and shade above.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q18::
Question ID:<br>ALG2_U02_L07_Q18<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>For y >= -x<sup>2</sup> + 1, which statement is true? {
~ Use a dashed boundary and shade above.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= Use a solid boundary and shade above.#Correct. The symbol >= includes the boundary and represents y-values above it.
~ Use a solid boundary and shade below.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ Use no boundary.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q19::
Question ID:<br>ALG2_U02_L07_Q19<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Solve x<sup>2</sup> - 6x + 8 = 0. {
~ x = -2 and x = -4#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x = 6 and x = 8#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= x = 2 and x = 4#Correct. Factoring gives (x - 2)(x - 4) = 0.
~ x = 0 and x = 8#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q20::
Question ID:<br>ALG2_U02_L07_Q20<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Solve x<sup>2</sup> + x - 12 = 0. {
~ x = 4 and x = -3#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x = 1 and x = -12#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x = 0 and x = -12#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= x = -4 and x = 3#Correct. Factoring gives (x + 4)(x - 3) = 0.
}

::ALG2_U02_L07_Q21::
Question ID:<br>ALG2_U02_L07_Q21<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Which solves x<sup>2</sup> - 16 > 0? {
= x < -4 or x > 4#Correct. The upward-opening graph is above the x-axis outside the zeros.
~ -4 < x < 4#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ -4 <= x <= 4#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ all real numbers#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q22::
Question ID:<br>ALG2_U02_L07_Q22<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Which solves x<sup>2</sup> - 25 <= 0? {
~ x < -5 or x > 5#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= -5 <= x <= 5#Correct. The graph is on or below the x-axis between and including the zeros.
~ x <= -5 or x >= 5#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ all real numbers#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q23::
Question ID:<br>ALG2_U02_L07_Q23<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>For y < x<sup>2</sup> - 3, which statement is true? {
~ Use a solid boundary and shade below.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ Use a dashed boundary and shade above.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= Use a dashed boundary and shade below.#Correct. Strict inequality uses a dashed boundary, and y less than means below.
~ Use a solid boundary and shade above.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

::ALG2_U02_L07_Q24::
Question ID:<br>ALG2_U02_L07_Q24<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>For y >= -x<sup>2</sup> + 1, which statement is true? {
~ Use a dashed boundary and shade above.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ Use a solid boundary and shade below.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ Use no boundary.#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
= Use a solid boundary and shade above.#Correct. The symbol >= includes the boundary and represents y-values above it.
}

::ALG2_U02_L07_Q25::
Question ID:<br>ALG2_U02_L07_Q25<br><br>MLA Standard:<br>MLA.A2.PF.2; MLA.A2.PF.5; MLA.A2.PF.6<br><br>Question:<br>Solve x<sup>2</sup> - 6x + 8 = 0. {
= x = 2 and x = 4#Correct. Factoring gives (x - 2)(x - 4) = 0.
~ x = -2 and x = -4#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x = 6 and x = 8#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
~ x = 0 and x = 8#This answer does not match the equation, inequality symbol, or graph region. Recheck zeros, boundary inclusion, and whether the graph is above or below the x-axis.
}

